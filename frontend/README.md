# 📝 Task Manager - Frontend

Frontend React do Sistema de Gerenciamento de Tarefas.

## 🚀 Desenvolvimento

```bash
npm install
npm run dev
```

## 🔧 Build para Produção

```bash
npm run build
```

## 🌐 Variáveis de Ambiente

Crie um arquivo `.env` na raiz:

```env
VITE_API_URL=http://localhost:8000/api
```

Para produção, configure com a URL da sua API.

## 📦 Deploy na Vercel

1. Conecte o repositório na Vercel
2. Configure a variável `VITE_API_URL` no painel
3. Deploy automático a cada push
